---
import AppLayout from '../components/layout/AppLayout.astro';
import { SYLLABLE_COUNT, TOTAL_COMBINATIONS } from '../data/pinyinSyllables';
---

<AppLayout title="Home" currentSection="home">
  <div class="hero">
    <img
      src="/pinyin-chart/pinyinpanda.jpg"
      alt="Cute panda mascot with glasses reading a pinyin chart"
      class="hero-mascot"
    />
    <h1 class="hero-title">Master Mandarin Pinyin</h1>
    <p class="hero-subtitle">
      Learn Chinese pronunciation through interactive soundboards and scientifically-designed quizzes.
    </p>

    <div class="cta-buttons">
      <a href="/pinyin-chart/soundboard" class="cta-button cta-primary">
        🔊 Try Soundboard
      </a>
      <a href="/pinyin-chart/quiz/tone" class="cta-button cta-secondary">
        🎮 Start Quizzes
      </a>
    </div>
  </div>

  <div class="features">
    <div class="feature-card">
      <div class="feature-icon">🎵</div>
      <h3>Interactive Soundboard</h3>
      <p>Explore all {SYLLABLE_COUNT}+ pinyin syllables with {TOTAL_COMBINATIONS} tone combinations. Click to hear native pronunciation.</p>
    </div>

    <div class="feature-card">
      <div class="feature-icon">🎯</div>
      <h3>Progressive Quizzes</h3>
      <p>6 scientifically-designed quiz types from tone recognition to complete syllables. Unlock levels as you improve.</p>
    </div>

    <div class="feature-card">
      <div class="feature-icon">💾</div>
      <h3>Offline Support</h3>
      <p>Audio files cached in your browser. Works offline after first use. No downloads required!</p>
    </div>

    <div class="feature-card">
      <div class="feature-icon">📊</div>
      <h3>Track Progress</h3>
      <p>Monitor your learning journey. See quiz scores, accuracy rates, and syllables mastered.</p>
    </div>
  </div>

  <div class="about">
    <h2>Why Pinyin Matters</h2>
    <p>
      Pinyin is the romanization system for Mandarin Chinese. Mastering pinyin pronunciation—especially the four tones—is
      crucial for clear communication and avoiding embarrassing mix-ups (like saying "horse" instead of "mother").
    </p>

    <h3>Pedagogically Validated Approach</h3>
    <ul>
      <li><strong>Audio-first learning:</strong> Listen before speaking, just like native learners</li>
      <li><strong>Minimal pairs drills:</strong> Distinguish confusing sounds (shi vs si, an vs ang)</li>
      <li><strong>Progressive difficulty:</strong> Start simple, build to complex syllables</li>
      <li><strong>Spaced repetition:</strong> Active recall through quizzes improves retention</li>
    </ul>

    <p class="about-cta">
      Ready to begin? <a href="/pinyin-chart/soundboard">Start with the soundboard →</a>
    </p>
  </div>
</AppLayout>

<style>
  .hero {
    text-align: center;
    padding: var(--spacing-2xl) 0;
    margin-bottom: var(--spacing-2xl);
  }

  .hero-mascot {
    width: 300px;
    height: auto;
    margin-bottom: var(--spacing-lg);
  }

  .hero-title {
    font-size: var(--font-size-4xl);
    background: linear-gradient(135deg, var(--tone-1-color), var(--tone-3-color));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: var(--spacing-md);
  }

  .hero-subtitle {
    font-size: var(--font-size-xl);
    color: var(--color-text-secondary);
    max-width: 600px;
    margin: 0 auto var(--spacing-xl);
  }

  .cta-buttons {
    display: flex;
    gap: var(--spacing-md);
    justify-content: center;
    flex-wrap: wrap;
  }

  .cta-button {
    padding: var(--spacing-md) var(--spacing-xl);
    border-radius: var(--radius-lg);
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-semibold);
    text-decoration: none;
    transition: all var(--transition-fast);
  }

  .cta-primary {
    background-color: var(--color-primary);
    color: white;
  }

  .cta-primary:hover {
    background-color: var(--color-primary-hover);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }

  .cta-secondary {
    background-color: var(--color-surface);
    color: var(--color-primary);
    border: 2px solid var(--color-primary);
  }

  .cta-secondary:hover {
    background-color: var(--color-primary);
    color: white;
    transform: translateY(-2px);
  }

  .features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--spacing-lg);
    margin-bottom: var(--spacing-2xl);
  }

  .feature-card {
    background-color: var(--color-surface);
    padding: var(--spacing-xl);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    transition: all var(--transition-fast);
  }

  .feature-card:hover {
    box-shadow: var(--shadow-md);
    transform: translateY(-2px);
  }

  .feature-icon {
    font-size: 3rem;
    margin-bottom: var(--spacing-md);
  }

  .feature-card h3 {
    color: var(--color-primary);
    margin-bottom: var(--spacing-sm);
  }

  .feature-card p {
    color: var(--color-text-secondary);
    margin: 0;
  }

  .about {
    background-color: var(--color-surface);
    padding: var(--spacing-2xl);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    margin-bottom: var(--spacing-2xl);
  }

  .about h2 {
    color: var(--color-primary);
  }

  .about ul {
    margin-bottom: var(--spacing-lg);
  }

  .about li {
    margin-bottom: var(--spacing-md);
  }

  .about-cta {
    font-size: var(--font-size-lg);
    text-align: center;
  }

  .about-cta a {
    font-weight: var(--font-weight-semibold);
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: var(--font-size-3xl);
    }

    .hero-subtitle {
      font-size: var(--font-size-lg);
    }

    .features {
      grid-template-columns: 1fr;
    }
  }
</style>
