---
/**
 * Tone Recognition Quiz Page
 */

import AppLayout from '../../components/layout/AppLayout.astro';
import BaseQuiz from '../../components/quiz/BaseQuiz';
import '../../styles/quiz.css';

const title = 'Tone Recognition Quiz';
const description = 'Listen to syllables and identify which tone is being used.';
---

<AppLayout title={title} currentSection="quiz">
  <BaseQuiz quizId="tone-recognition" client:load />
</AppLayout>
